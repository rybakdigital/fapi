routes:
    readProduct:
        path:   v1/products/{productName}/category/{category}
        methods: ['GET', 'POST']
        controller: orders
        calls: readRequest
        requirements:
            productName: str
            category:   str
